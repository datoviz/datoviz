# C Composite Examples

Semantic scene objects belong here when they are larger than one visual family but still small,
copy-safe building blocks. A composite owns domain semantics and lowers to one or more visuals.

- `polygon.c`: semantic polygon and polygon-set composite.
- `graph.c`: semantic graph composite with marker nodes and curved edges.
