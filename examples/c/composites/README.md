# C Composite Examples

Public composite examples belong here. Composites are semantic scene objects that lower to one or
more built-in visuals; they are not visual families.

- `polygon.c`: polygon and polygon-set composites with fill/stroke roles.
- `graph.c`: clustered graph composite with marker nodes and Bezier path edges.
