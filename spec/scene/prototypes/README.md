# Scene Prototype APIs

Files in this directory are exploratory API sketches for the future scene layer.

They are not the public API and they should not be treated as implementation-ready contracts.
The source of truth for the planning phase is the markdown material under `spec/scene/`.
