# Scene API Sketch (superseded)

This document is superseded by `spec/scene/headers/scene_api.h`, which is the authoritative
informative C header for the v0.4 scene API.

All design decisions previously sketched here have been resolved and are reflected in
`scene_api.h` and the per-topic spec documents in `spec/scene/`.
