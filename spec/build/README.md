# Build Specs

This directory owns durable build and packaging contracts.


## Index

1. [COMPONENT_TARGETS.md](COMPONENT_TARGETS.md): component-target product model, dependency rules,
   and remaining packaging hardening work.


## Boundary

Use this directory for build-system contracts, CMake target boundaries, install/export policy, and
package-consumer validation. Put source dependency semantics in `../architecture/` and runtime
protocol behavior in `../drp2/`.
