features/panel_domain_fit
- grid appears in front of the visual, that isn't expected right?
- panzoom on the right panel is not aspect constrained?
- when resizing vertically the x axis and the x axis label become clipped by the bottom border

features/selection_mesh_instances
- doesn't appear to work

features/selection_pixel
- y is flipped so when i hover on a pixel on the bottom, a pixel at the top (symmetrically) is being highlighted

features/selection_sphere
- doesn't appear to work

features/technique_depth_test
- is kind of broken, redo it properly in 3D, maybe with several sphere meshes?

features/technique_edl
- i don't see any difference between the two panels
- i want the labels to be msdf, i want this technique to be the default, especially when agents write code, also the font size is too small, there needs to be example wide helpers so that all examples use larger font size and msdf for labels

features/technique_ssao
- the effect settings are badly calibrated, i want a temporary gui so that i can choose the best settings for the sake of the demo, i will also want to tweak the arcball params manually and record those i choose

features/technique_transparency
- very slow and seems badly broken, text needs to use msdf

features/triangulation_polygon
- i am confused as what this is supposed to show
- i would have imagined a single panel with a polygon and the triangulation that is visible with segments for the triangles making up the triangulation
- i would want a similar example like features/triangulation_pslg.c

features/user_scale
- the axis segment width should also be scaled
- the margin around the panel should also scale

showcases/gpu_particle_smoke
- discrepancy between the mouse position and the effect on the panel

showcases/point_cloud
- two finger scroll on macOS to move the camera is way too sensitive
- space/control is not sensitive enough
- i wonder if the speed of ctrl/space vertical scroll shouldn't accelerate when the height absolute value increases? what is standard?

showcases/textured_planet
- orientation is totally wrong, re-check everything

visuals/glyph
- i do not want to see the atlas on the right at all, just the glyph, the example should really document how to use the low level glyph visual and how to use the atlas, font etc

visuals/vector
- broken, all magenta panel
scenario_runner: visual_vector requirements: controller,panzoom
20:11:33.109 T450 E              app.c:2714: _app_draw runtime execution failed: code=3 (OutOfRange) command=95/101 type=27 (SetVertexBuffer) id=35(v2_path_distance)



with scalebar in 3D and arcball, the scalebar should only depend on the camera position, and should NOT change when rotating the arcball because that only affects the model matrix



implement spec/scene/proposals/active/EQUAL_ASPECT_PANEL_VIEW.md





