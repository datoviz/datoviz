# Queries

Draft reference stub.

Exact pick/probe/query API details are pending the active query overhaul. Keep this page as a
placeholder until public request, callback, payload, fallback, and lifetime rules are finalized.
