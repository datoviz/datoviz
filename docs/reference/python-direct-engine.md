# Python API

The NumPy-adapted and exact `ctypes` call forms are now documented together in the
[Python API reference](ctypes.md).

Datoviz has one generated Python binding: use `import datoviz as dvz` for normal NumPy-adapted
calls and `import datoviz.raw as raw` for exact pointers, counts, byte sizes, and callbacks.
