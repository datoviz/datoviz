# Next steps

- Update `docs/tasks/README.md` opportunistically when adding new task directories or superseding recent ones.
- If the task history grows substantially, consider grouping the index into `active`, `recent completed`, and `superseded` sections.
- If the repo later wants stronger policy visibility, add a short cross-reference from broader contributor/agent docs to `docs/tasks/README.md`.
