# Next steps

- No immediate code follow-up is required.
- If wheel behavior ever changes in another backend, keep the same sign convention at the input
  layer before it reaches scene controllers.
- Preserve the single cross-platform expectation in `test_panzoom_zoom_wheel`.
