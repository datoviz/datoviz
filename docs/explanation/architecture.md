# Architecture

Draft explanation stub.

Datoviz v0.4 uses a `scene` -> `drp2` -> `vklite`/`canvas` runtime path with `app` as a small
presentation layer. This page should explain that stack and the boundary between public APIs,
command streams, and backend execution.
