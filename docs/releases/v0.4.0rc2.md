# Datoviz v0.4.0rc2

v0.4.0rc2 is the active release candidate and a narrow replacement for RC1. It fixes the packaged
macOS native-window Vulkan-loader mismatch while preserving the v0.4 architecture and API scope.

## Publication status

All six RC2 wheels passed the complete candidate campaign and are byte-for-byte verified on
TestPyPI. Production PyPI and GitHub release publication remain pending explicit maintainer
approval. After publication, install the exact package with:

```sh
python -m pip install --pre datoviz==0.4.0rc2
```

Until then, build `v0.4-dev` from source for native-window testing. The published RC1 macOS wheel
retains the native-window limitation recorded in the [RC1 release notes](v0.4.0rc1.md).

## Validation scope

RC2 requires fresh hosted build, wheel, clean-install, shader, consumer, and rendering evidence for
all six wheel targets. Hosted Linux also exercises the installed Quickstart through Xvfb, while
hosted macOS uses offscreen MoltenVK proof because hosted desktop-window sessions are unreliable.

The exact canonical macOS arm64 wheel must additionally pass an attended native-window Quickstart
on the available MacBook M3, including resize, pan, and zoom. Physical Linux and Windows hosts are
unavailable for this candidate and are recorded as explicit exclusions, not passes. Their physical
coverage returns for RC3 and the final release.

## Release evidence

| Evidence | Result |
| --- | --- |
| Source Test [`29695441493`](https://github.com/datoviz/datoviz/actions/runs/29695441493) | Linux, macOS, and Windows passed at the wheel artifact commit. |
| Canonical Wheels [`29695746332`](https://github.com/datoviz/datoviz/actions/runs/29695746332) | All six wheels, inspections, and Python 3.10–3.14 install smokes passed; the Python 3.15 prerelease smoke also passed. |
| Hosted conformance [`29696169890`](https://github.com/datoviz/datoviz/actions/runs/29696169890) | All six installed-wheel lanes and the aggregate report passed. |
| Physical intake [`29697580837`](https://github.com/datoviz/datoviz/actions/runs/29697580837) | The exact macOS arm64 wheel passed two unattended profiles and the complete attended interaction set on the MacBook M3. |
| TestPyPI verification [`29698900673`](https://github.com/datoviz/datoviz/actions/runs/29698900673) | All six indexed files matched canonical wheel bytes and passed clean installation checks. |
| Artifact-neutral follow-up Test [`29696633533`](https://github.com/datoviz/datoviz/actions/runs/29696633533) | Linux, macOS, and Windows passed after the release-tool argument-forwarding fix. |

The wheel artifact and conformance validator commit is `8765535db`. The only later source change
before the follow-up Test is an artifact-neutral release-tool wrapper fix; it does not affect wheel
bytes or runtime behavior.

## Canonical wheel checksums

| Target | SHA-256 |
| --- | --- |
| macOS arm64 | `1277afcf1d017b9c421e8d149ede6deabc82d859e3a67eb85c6ecbf82a8a8f59` |
| macOS x86_64 | `d3e10bea002d26d245e9f6f5b89c37a0ee3ec7a1c6e19136d93bbca390a96849` |
| Linux aarch64 | `46d220a5c56510433d6937da1b1003185a7439c95666df45b6f2307bf77b4da0` |
| Linux x86_64 | `ba0824db1d47fa55676193839a635aee79bca4cb0ebcf5c462b42a4411403595` |
| Windows AMD64 | `5ad18a1165fe8ffc0857ccd7d40d19ffa3528aaa230568975122a9d1b1aab951` |
| Windows ARM64 | `2e0b0d22234b0e0449e8ddbd60d328357fc2eb94b6c1012a159ce1db5e523471` |

## Known limitations

- Physical Linux and Windows machines were unavailable for RC2. This is an explicit exclusion,
  not a pass; fresh physical coverage returns for RC3 and the final release.
- Qt/PyQt hosting remains source-build-only because RC2 does not package a native Qt bridge
  provider. Packaged-provider work remains planned for RC3.
- Browser WebGPU and facilities labeled advanced/unstable remain experimental and are not promoted
  by this narrow native-window hotfix.
