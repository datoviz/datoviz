# Visual Gallery

Draft gallery stub.

This page should index one minimal C example per public visual family, including point, pixel,
marker, primitive, segment, path, image, mesh, sphere, volume, text, labels if released, and polygon
if kept public.
